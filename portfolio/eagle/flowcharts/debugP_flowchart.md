# debug 執行流程

```
/debugP 執行流程（v5 - 含 -f/-p 輔助模式）
│
├─ 0. 【建立進度追蹤】（強制）
│   └─ 根據情境使用 TaskCreate 建立追蹤項目
│
├─ 0.5 【-f/-p 模式：載入輔助知識】（條件執行）
│   │
│   ├─ 【-f 模式：檔案上傳輔助】
│   │   ├─ 檢測參數是否包含 -f flag？
│   │   │   ├─ 否 → 跳過
│   │   │   └─ 是 → 載入精簡知識（~2,500 tokens）
│   │   │       ├─ file_upload_architecture.md
│   │   │       └─ file_upload_checklist.md
│   │   └─ 輸出：「📁 檔案上傳輔助模式啟動」
│   │
│   └─ 【-p 模式：權限輔助】
│       ├─ 檢測參數是否包含 -p flag？
│       │   ├─ 否 → 跳過
│       │   └─ 是 → 載入精簡知識（~3,000 tokens）
│       │       └─ 2_permission_adjustment_rules.md
│       ├─ 輸出：「🔐 權限輔助模式啟動」
│       └─ 後續分析時識別：
│           ├─ 需要修改的權限檔案
│           ├─ 路由與權限對應關係
│           ├─ Guard 配置需求
│           └─ 輔助性 API 豁免判斷
│
├─ 1. 【讀取 Bug Spec】
│   └─ 完整讀取檔案內容
│
├─ 🆕 1.5 【問題盤點】（多問題 bug spec 必執行）
│   │
│   ├─ 1.5.1 檢測多問題結構（滿足任一即觸發）
│   │   ├─ 條件 1：檔案中有 2+ 個 `# [xxx]` 一級標題
│   │   ├─ 條件 2：檔案中有 2+ 個 `---` 分隔線
│   │   └─ 條件 3：🆕 留言區塊中有多問題描述（2026-02-01）
│   │       ├─ 數字列表、「有兩個/三個地方」
│   │       ├─ 連接詞：「另外」「還有」「以及」
│   │       └─ 明確陳述：「這邊有 N 個問題」
│   │
│   ├─ 1.5.2 逐一盤點問題
│   │   ├─ 提取每張票的標題
│   │   ├─ 識別每張票的核心問題
│   │   ├─ 🆕 **分析錯誤訊息含義**（2026-02-01）
│   │   │   ├─ 識別 bug spec 中的實際錯誤訊息
│   │   │   ├─ 區分「表象問題」vs「根因問題」
│   │   │   └─ 若根因問題與表象問題不同 → 視為獨立問題
│   │   ├─ 判斷問題是否相同或相關
│   │   └─ 判斷是否需要 DB 驗證
│   │
│   └─ 1.5.3 輸出問題清單表格
│       │
│       │  | # | 票標題 | 核心問題 | 需要 DB | 是否相同根因 |
│       │  |---|--------|---------|---------|-------------|
│       │  | 1 | ... | ... | ❌ | - |
│       │  | 2 | ... | ... | ✅ | ✅/❌ |
│       │
│       └─ 後續分析必須涵蓋所有問題
│
├─ 1.6 【帳號智能識別】
│   │
│   ├─ 掃描整份 Bug Spec（主內容 + 留言區塊）
│   │   └─ 正則匹配：account/password 格式
│   │
│   ├─ 識別結果：
│   │   ├─ 找到 1 個帳號 → 記錄，後續使用
│   │   ├─ 找到多個帳號 → 記錄所有，準備迭代驗證
│   │   └─ 沒找到 → 使用預設帳號
│   │       ├─ 指令：get-token.sh $1 12345678
│   │       └─ ⚠️ 預設帳號帳密相同，禁止加 /password
│   │
│   └─ 輸出：
│       └─ 「📌 發現 N 個測試帳號：{帳號列表}」
│
├─ 🆕 1.8 【檢查已有 Proposal】（2026-02-02 新增）
│   │
│   ├─ 搜尋 bug spec 同目錄是否有 *_proposal.md
│   │   └─ ls $(dirname bug_spec_path)/*_proposal.md
│   │
│   ├─ 有找到 → 讀取 proposal
│   │   ├─ 了解之前的分析結果
│   │   ├─ 了解之前的修改方案
│   │   └─ 了解驗證結果（成功/失敗）
│   │
│   └─ 後續分析時參考 proposal 內容
│       └─ 避免重複分析已知的問題
│
├─ 2. 【場景判斷】（AI 智能判斷，不需詢問用戶）
│   ├─ 對話上下文有 /implement → 改用 /check-result 驗證
│   └─ 單獨 /debugP 查問題 → 直接打遠端 API
│       ├─ API：https://api-{$1}.<COMPANY_DOMAIN>
│       └─ 不需要切 DB、不需要啟動本地 server
│
├─ 3. 【條件判斷】
│   ├─ 路徑含 frontPage_api/debug/ → 載入 frontPage 歷史開發紀錄
│   └─ 標題/內容含 "500" → ⚠️ 強制優先執行 PM2 流程
│       └─ PM2 日誌 → Stack Trace → 程式碼 → DB 驗證
│
├─ 🆕 3.5 【-p 模式專屬分析】（僅 -p 模式執行）
│   │
│   ├─ 3.5.1 分析權限配置（必做）
│   │   ├─ permissions.constant.ts（路由有沒有設權限）
│   │   ├─ apiPermissions.constant.ts（權限有沒有定義）
│   │   └─ rolePermissionMapping.constant.ts（角色有沒有被授權）
│   │
│   ├─ 3.5.2 判斷權限問題類型（基於配置分析 + 錯誤訊息）
│   │   │
│   │   ├─ A. 沒權限（配置缺失或沒授權）
│   │   │   ├─ 特徵：403、"permission"、"forbidden"
│   │   │   └─ 後續：直接產出解法，跳過 DB/API 驗證
│   │   │
│   │   └─ B. 有權限卻被擋（配置正確但還是 403）
│   │       ├─ 特徵：資料被過濾、看不到特定資料
│   │       └─ 後續：繼續走 DB + API 驗證流程
│   │
│   └─ 輸出：
│       ├─ A 類型 →「🔐 權限配置問題，跳過 DB 驗證」
│       └─ B 類型 →「🔐 需要 DB 驗證資料隔離邏輯」
│
├─ 🆕 4.0 【查 API Data Flow 文件】（條件）
│   │
│   ├─ 判斷是否為 API 相關 bug
│   │   └─ 依據：bug spec 中的 API 路徑、endpoint、controller 名稱等
│   │
│   ├─ 【是 API 相關】→ 提取 apiName
│   │   ├─ 搜尋 6_api_data_flow/{adminApi|publicApi}/{apiName}-data-flow.md
│   │   ├─ 找到 → 讀取，作為後續分析的基礎知識
│   │   └─ 沒找到 → ⚠️ 中斷，提示先執行 /api-flow-architecture {apiName}
│   │
│   └─ 【非 API 相關】→ 跳過（migration、config、util 等）
│
├─ 4. 【分析與驗證 - 分流執行】
│   │
│   ├─ 4.1 先處理：不需要 DB 的問題
│   │   ├─ 查看前端程式碼
│   │   ├─ 查看後端 API、程式碼、entity
│   │   └─ 產出解法（純程式碼修改）
│   │
│   ├─ 4.2 條件執行：DB 連線（只連一次）
│   │   │
│   │   ├─ 檢查：問題清單中是否有任一問題需要 DB？
│   │   │   ├─ 否 → 跳過 DB 相關步驟
│   │   │   └─ 是 → 繼續執行
│   │   │
│   │   ├─ 先檢查連線狀態：./scripts/db-tunnel.sh status
│   │   ├─ 已連線目標環境 → 跳過切換
│   │   └─ 未連線 → 執行 db-tunnel.sh $1
│   │
│   └─ 4.3 後處理：需要 DB 的問題
│       ├─ 查詢相關資料表
│       ├─ 驗證資料狀態
│       ├─ 分析錯誤觸發原因
│       └─ 產出解法（含 DB 驗證結果）
│
├─ 5. 【帳號驗證】（🆕 2026-02-02 修正策略）
│   │
│   ├─ 5.0 判斷驗證範圍
│   │   ├─ bug spec 有指定帳號 → 只驗證指定的帳號
│   │   │   例：「店長進入物調表跳錯誤」→ 只驗證店長
│   │   └─ bug spec 沒指定（如「所有非總部帳號」）→ 全部 role 驗一輪
│   │
│   ├─ 5.1 查前端索引表（ui-api-index）
│   │   ├─ Dashboard 相關 → 查 ui-api-index-dashboard.md
│   │   ├─ Frontend 相關 → 查 ui-api-index-frontend.md
│   │   └─ 找到 bug spec 操作對應的 API
│   │
│   ├─ 5.2 取得 Token
│   │   ├─ 輸出：「📌 驗證帳號：{account}（{角色}）」
│   │   └─ ./.claude/scripts/get-token.sh $1 {account/password}
│   │
│   ├─ 5.3 打遠端 API 驗證（優先）
│   │   ├─ ⚠️ 統一使用 test-dashboard-api.sh（禁止裸 curl）
│   │   ├─ ./.claude/scripts/test-dashboard-api.sh $1 "/path" ".jq"
│   │   ├─ 分析回傳結果
│   │   └─ 驗證現況（錯誤訊息、行為）
│   │
│   ├─ 5.4 查 DB（備用，需要時才做）
│   │   ├─ 需要理解資料狀態時才執行
│   │   ├─ 方式 1：SSH 進機器查
│   │   └─ 方式 2：本地 db-tunnel.sh 連遠端 DB
│   │
│   ├─ 5.5 檔案可存取性驗證（條件觸發）
│   │   └─ API 回傳含 fileUrl/files 欄位時執行
│   │
│   └─ 5.6 記錄驗證結果
│       └─ 暫存結果，待產出提案時統一寫入
│
├─ 🆕 6. 【產出提案】（多問題格式）
│   │
│   ├─ 6.1 問題清單表格（開頭）
│   │   │
│   │   │  ## 問題清單
│   │   │  | # | 票標題 | 核心問題 | 需要 DB | 解法章節 |
│   │   │  |---|--------|---------|---------|---------|
│   │   │  | 1 | ... | ... | ❌ | § 解法 1 |
│   │   │  | 2 | ... | ... | ✅ | § 解法 2 |
│   │   │
│   │   └─ 標註根因關聯（若有）
│   │
│   ├─ 6.2 各問題解法（分章節）
│   │   ├─ ## 解法 1：[問題 1 標題]
│   │   │   ├─ 問題來源表格
│   │   │   └─ 修改內容（純程式碼）
│   │   │
│   │   └─ ## 解法 2：[問題 2 標題]
│   │       ├─ 問題來源表格
│   │       ├─ DB 驗證結果（如有）
│   │       └─ 修改內容
│   │
│   └─ 6.3 包含所有帳號的驗證紀錄
│
│   └─ 🆕 6.4 分頁預設值檢查（條件執行）
│       ├─ 涉及 findAll/list endpoint？
│       │   ├─ 否 → 跳過
│       │   └─ 是 → 檢查 DTO pageSize/limit 預設值
│       │       ├─ 不是 10 → 在 proposal 加入修改建議
│       │       └─ 是 10 → 不提
│
├─ 7. 【檢核階段】
│   ├─ 7.1 歷史文件驗證（搜尋相關歷史決策）
│   ├─ 7.2 依照檢核規則檢查提案文件
│   │
│   ├─ 7.3 【-f 模式專屬檢核】（條件執行）
│   │   │
│   │   ├─ 檢測參數是否包含 -f flag？
│   │   │   ├─ 否 → 跳過
│   │   │   └─ 是 → 執行 6 項檔案上傳專屬檢核
│   │   │
│   │   └─ 6 項專屬檢核：
│   │       ├─ CUSTOM_PATH_PREFIX
│   │       ├─ DOCUMENT_TYPE_MAPPING
│   │       ├─ Module 設定
│   │       ├─ Service 方法
│   │       ├─ DTO 欄位
│   │       └─ Entity 關聯
│   │
│   └─ 7.4 【-p 模式專屬檢核】（條件執行）
│       │
│       ├─ 檢測參數是否包含 -p flag？
│       │   ├─ 否 → 跳過
│       │   └─ 是 → 執行 6 項權限專屬檢核
│       │
│       └─ 6 項專屬檢核：
│           ├─ apiPermissions.constant.ts（權限項目定義）
│           ├─ permissions.constant.ts（路由權限對應）
│           ├─ rolePermissionMapping.constant.ts（角色集合）
│           ├─ Controller Guards（@UseGuards 順序）
│           ├─ Module 依賴（Entity、Redis、Guard）
│           └─ 輔助性 API 豁免（simple-list 等）
│
└─ 8. 【結束前檢查】（強制）
    ├─ 執行 TaskList 確認所有 task 狀態
    ├─ 全部 completed → 輸出結果
    └─ 有未完成 → 先補完成再輸出
│
└─ 9. 【更新進度表】（@update-progress.md）
    └─ 將 /debugP 標記為 ✅
```

## DB 需求判斷規則

| 問題類型 | 需要 DB | 原因 |
|---------|---------|------|
| 錯誤訊息中文化 | ❌ | 純程式碼修改，看 service 即可 |
| 🆕 錯誤訊息內容暗示資料問題 | ✅ | 訊息內容暗示資料/邏輯問題，需查 DB 驗證為何觸發此錯誤 |
| 資料顯示錯誤 | ✅ | 需查 DB 驗證資料狀態 |
| 儲存失敗 | ✅ | 需查 DB 驗證 ID 是否存在 |
| 權限問題（沒權限） | ❌ | 看權限配置即可 |
| 權限問題（有權限卻被擋） | ✅ | 需查 DB 驗證資料隔離 |
| UI 顯示異常（資料相關） | ✅ | 可能是欄位沒傳、格式錯誤，需查 DB |
| API 回傳格式錯誤 | ❌/✅ | 視情況，可能需查 DB 對比預期值 |

> ⚠️ **重要區分**（2026-02-01 新增）：
> - **錯誤訊息中文化**：只看訊息是中文還是英文
> - **錯誤訊息內容暗示資料問題**：分析訊息的「內容」是否暗示資料/邏輯問題

## 500 錯誤優先處理

> 💡 **500 Debug 流程口訣**：PM2 日誌 → Stack Trace → 程式碼 → DB 驗證

```
500 錯誤處理流程
│
├─ 1. SSH 連進指定環境
│
├─ 2. PM2 錯誤日誌查詢
│   └─ pm2 logs 0 --lines 50 --err（不要先 grep）
│
├─ 3. 找 Stack Trace
│   └─ 找到 Error 和 at xxx.ts:行數
│
├─ 4. 定位程式碼
│   └─ 根據 stack trace 讀對應的程式碼
│
└─ 5. 再驗證 DB
    └─ 用程式碼邏輯去驗證 DB 狀態
```

## Bug Fix 驗證三步法

```
驗證流程
│
├─ Step 1 - 推導 API 序列
│   ├─ 讀取「重現步驟」
│   ├─ 【優先】查詢 UI-API 索引
│   └─ 【備用】手動搜尋前端專案
│
├─ Step 2 - 存在性確認
│   ├─ 用 DB ID 打 findOne/findAll
│   └─ 比對 DB 預期值 vs API 回傳值
│
├─ Step 3 - Bug Spec 模擬
│   ├─ 照著操作步驟打 API
│   └─ 驗證現況
│
└─ Step 4 - 檔案可存取性驗證（條件觸發）
    ├─ 下載檔案
    ├─ 檢查格式和大小
    └─ 輸出驗證結果表格
```

## 多問題 Bug Spec 問題盤點流程

```
問題盤點流程
│
├─ 識別條件（滿足任一即觸發）
│   ├─ 檔案中有 2+ 個 `# [xxx]` 一級標題（排除 `## 留言`）
│   ├─ 檔案中有 2+ 個 `---` 分隔線（排除留言區塊前的分隔線）
│   └─ 🆕 留言區塊中有多問題描述（2026-02-01 新增）
│       ├─ 數字列表：「1. xxx 2. xxx」或「有兩個/三個地方」
│       ├─ 連接詞：「另外」「還有」「以及」「除此之外」
│       └─ 明確陳述：「這邊有 N 個問題」
│
│   > ⚠️ 設計理由：單票留言中可能補充額外問題，若只看標題會遺漏
│
├─ 盤點步驟
│   ├─ 提取每張票的標題
│   ├─ 識別核心問題
│   ├─ 🆕 **分析錯誤訊息含義**（2026-02-01）
│   │   ├─ 識別 bug spec 中的實際錯誤訊息
│   │   ├─ 區分「表象問題」vs「根因問題」
│   │   └─ 若根因問題與表象問題不同 → 視為獨立問題
│   ├─ 判斷 DB 需求
│   └─ 判斷根因關聯
│
└─ 分流執行
    ├─ 先處理：不需要 DB 的問題
    ├─ 條件：連線 DB（只連一次）
    └─ 後處理：需要 DB 的問題
```
