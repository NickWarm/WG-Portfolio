# 過渡期討論、求職準備資料 — 只留本機，不上 GitHub
workspace/

# 系統 / 編輯器
.DS_Store
*.swp
.vscode/
.idea/

# Claude Code 機器特定配置 — repo 內不放任何 .claude 配置
# （本 repo 自用 skill 真檔放在 workspace/skills/，靠 symlink 掛到 ~/.claude）
.claude/
